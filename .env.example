# Copy to .env.local if you want to drive local/public Expo config without editing tracked files.
EXPO_PUBLIC_APP_ENV=preview

# Optional after `npx eas project:init` if you prefer env-driven project linking in app.config.js.
# EAS_PROJECT_ID=

# Optional if you want to pin the Expo owner locally.
# EXPO_OWNER=
